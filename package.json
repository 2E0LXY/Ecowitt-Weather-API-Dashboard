{
  "name": "ecowitt-weather-dashboard",
  "version": "1.1.0",
  "private": true,
  "description": "Ecowitt weather dashboard packaged with Capacitor for Android.",
  "scripts": {
    "build": "node scripts/build-capacitor.js",
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync android",
    "android:apk": "npm run build && npx cap add android && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.4.1",
    "@capacitor/core": "8.4.1",
    "@capacitor/geolocation": "8.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.1"
  }
}
